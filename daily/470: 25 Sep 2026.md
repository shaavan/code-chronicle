# Log 470: Saved State, Loaded State

> 25 September 2026

## What I worked on

- Reviewed the first [recurrence store commits][recurrence-branch] and caught a private `RecurrenceStore` import in `bolt12.rs`; `cargo check --lib` confirmed the `E0603` error, which was fixed in the updated feature commit.
- Focused coverage on the two persistence boundaries: recurrence ID in the serialization roundtrip, and saved records restored when building a node.
- Added a builder test that loads two persisted recurrences and checks their IDs, statuses, offer data, paid counts, and opaque state.
- Ran formatting and `git diff --check`; tests remain unrun.

## Reflection

The first review surfaced a compile error, then the stack changed and the import was corrected before the next pass. Rereading the commits made the missing coverage easier to name: the existing roundtrip needed to prove it kept the recurrence ID, and node construction needed to prove it restored more than one saved recurrence with its state intact.

That gave the tests a useful boundary. The store already had generic behavior coverage, while recurrence execution is still disabled, so separate update or payment execution tests would reach past what these commits provide. The builder test now follows persisted records into a constructed node; the roundtrip checks identity on the way through serialization. I left test execution for later, so the coverage is in place but still needs a run.

[recurrence-branch]: https://github.com/shaavan/ldk-node/commits/node-recurrence-A-03
